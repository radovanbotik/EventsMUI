import { Box, Typography } from "@mui/material";

const Logo = () => {
  return (
    <Box>
      <Typography fontWeight="700" variant="h5">
        Events.
      </Typography>
    </Box>
  );
};

export default Logo;
