import { Typography } from "@mui/material";

const Logo = () => {
  return (
    <Typography variant="h6" component="div" sx={{ display: "block", fontWeight: 700, my: 2 }}>
      Events.
    </Typography>
  );
};

export default Logo;
